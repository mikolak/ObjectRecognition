# Add all components here using ADD_COMPONENT(<COMPONENT_DIRECTORY>)
ADD_COMPONENT(ConvolutionFilter)

ADD_COMPONENT(ContrastManipulation)

ADD_COMPONENT(MedianBlurFilter)

ADD_COMPONENT(Thresholding)

ADD_COMPONENT(LogicFilter)

ADD_COMPONENT(RankFiltering)

ADD_COMPONENT(NewestImage)

ADD_COMPONENT(BrightnessManipulation)

ADD_COMPONENT(DrawDetectedCircles)
